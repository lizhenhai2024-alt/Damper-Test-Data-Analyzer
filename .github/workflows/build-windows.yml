name: Build Windows EXE

on:
  workflow_dispatch:
  push:
    branches: ["dev-*"]
    paths:
      - "src/**"
      - "tools/**"
      - "launcher.py"
      - "pyproject.toml"
      - ".github/workflows/build-windows.yml"

jobs:
  build:
    runs-on: windows-latest
    env:
      APP_VERSION: V0.9.5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[gui]" pyinstaller pillow
      - name: Render application icon
        run: python tools/render_app_icon.py
      - name: Build executable
        run: |
          pyinstaller --noconfirm --clean --windowed --onefile --name "Damper_Test_Data_Analyzer_$env:APP_VERSION" --icon "src/cdc_analyzer/assets/damper_test_data_analyzer.png" --paths src --collect-all pyqtgraph --hidden-import pyqtgraph.exporters --add-data "src/cdc_analyzer/assets;cdc_analyzer/assets" launcher.py
      - name: Upload executable
        uses: actions/upload-artifact@v4
        with:
          name: Damper_Test_Data_Analyzer_${{ env.APP_VERSION }}_Windows
          path: dist/Damper_Test_Data_Analyzer_${{ env.APP_VERSION }}.exe
          if-no-files-found: error
